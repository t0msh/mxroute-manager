# MXRoute Api Details
MX_SERVER=your_mxroute_server_domain (e.g. blizzard.mxrouting.net)
MX_USER=your_mxroute_username
MX_API_KEY=your_mxroute_api_key

# Cloudflare Automatic DNS Records
CF_API_TOKEN=your_cloudflare_api_token
CF_ACCOUNT_ID=your_cloudflare_account_id

# OIDC Authentication configuration
OIDC_ENABLED=true
OIDC_CLIENT_ID=your_oidc_client_id
OIDC_CLIENT_SECRET=your_oidc_client_secret
OIDC_DISCOVERY_URL=https://auth.t0m.sh/.well-known/openid-configuration
OIDC_REDIRECT_URI=https://your-domain.com/oidc/callback
OIDC_SCOPES=openid email profile groups
OIDC_ADMIN_USERS=admin1@example.com,admin2@example.com
OIDC_ADMIN_GROUP=administrators
SECRET_KEY=generate_a_long_random_string_for_session_encryption
# Set to true when serving over HTTPS (required for local-auth deployments behind TLS)
FORCE_HTTPS=false
# DMARC TXT value used for Cloudflare auto-setup and health checks
DMARC_RECORD=v=DMARC1; p=none; sp=none; adkim=r; aspf=r;
# Directory for daily audit log files (logs/YYYY-MM-DD.log)
LOG_DIR=./logs

# Local Admin credentials (used if OIDC is enabled as a fallback login option)
ADMIN_USER=admin
ADMIN_PASSWORD=change_this_to_a_secure_password
